metadata:
  name: testpolicy
  namespace: test
spec:
  tags: "MITRE"
  message: "Warning...."
  selector:
    matchLabels:
      app: testpolicy
  file:
    matchPaths:
      - path: /tmp
    matchDirectories:
      - dir: /tmp
        recursive: true
    severity: 1
  Action: deny
